/* ---------------------------------------------------------------------- */
/* Condition                                                              */
/* ---------------------------------------------------------------------- */

let answer = +prompt('숫자를 입력해주세요', 0);
let result = answer > 0 ? 1 : answer < 0 ? -1 : 0;
// if (answer > 0) {
console.log(result);
// } else if (answer < 0) {
//   conso3le.log(result);
// } else {
//   console.log(result);
// }

// 그 영화 봤니?
//     ↓
// Yes | No
//     | 영화 볼거니?
//           ↓
//       Yes | No

// 영화 봤니?
let didWatchMovie;

// 영화 볼거니?
let goingToWatchMovie;

// if 문(statement)

// else 절(caluse)

// else if 복수 조건 처리

// 조건부 연산자

// 멀티 조건부 연산자 식
